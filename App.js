//import liraries
import React, { Component } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import BottomTabNavigation from './src/navigation/bottomstack';
// create a component
const MyComponent = () => {
  return (
   <BottomTabNavigation />
  );
};

export default MyComponent;
